<div class="home_page">
  <h1 align="center"> Welcome to the Facemash!  </h1>
  <% if user_signed_in? %>
  <div align="center" ><%= link_to "Go to battles", battles_path,
                    class: "btn btn-primary btn-lg btn-xlarge" %>
  <% else %>
    <div align="center" ><%= link_to "Sign up now", new_user_registration_path,
                    class: "btn btn-primary btn-lg btn-xlarge" %>
                    <%= link_to "Sign in now", new_user_session_path,
                    class: "btn btn-primary btn-lg btn-xlarge" %></div>
    </div>
  <% end %>
